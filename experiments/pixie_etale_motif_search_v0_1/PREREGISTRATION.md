# Preregistration: input-conditioned étale motif search

Status: `STAGED_NOT_AUTHORIZED`.

## Question

Do recurring local-gluing forms in the frozen Pixie LoRA response field survive
held-out prompts, paraphrases, nineteen norm-matched random adapters, targeted
interventions, and human transfer tasks well enough to organize mechinterp
learning or investigative craft?

## Frozen object

For module input `x` and compact update SVD `ΔW = UΣVᵀ`, mode energy is
`e_i = (σ_i v_iᵀx)^2`. X is response magnitude relative to the base module
output, Y is top-mode energy share, Z is entropy effective-mode fraction, and W
is transformer depth. A single discovery-only global scaler is applied before
any chart distance. No scaler is fitted per prompt, chart, radius, or window.

The seven module identities remain globally distinct sheets. At each depth and
radius, all 21 pairwise distances are exact edge births. Epsilon cuts expose
direct edges and their single-linkage closure. Chain excess, Tarjan bridges and
articulation vertices, bridge-free status, dendrograms, bands, and quotient
events are stored without smoothing.

## Splits and controls

The deterministic 192-input corpus contains six families, eight semantic groups
per family, and four variants per group. Whole groups are assigned 96/48/48 to
discovery, confirmation, and transfer. Mining is blind to family and behavior.
Nineteen random adapters match every module's effective delta Frobenius norm
and are evaluated on the same saved base activations.

The smallest Ward `k` in 2–8 is frozen only if silhouette is at least 0.25,
group-subsample stability is at least 0.70, and at least two clusters contain
eight semantic groups. Otherwise the primary outcome is `NO_STABLE_MOTIFS`.

## Interpretation

Confirmation can mint only a descriptive motif. Predictive value, random-null
specificity, targeted causal actionability, learning utility, and craft utility
are independent badges. A negative or unavailable result is primary evidence.
No context loop is registered, so S and monodromy remain unavailable.
